## 2026-08-30 (Day 1)：打通AI大模型通信链路

### ① 我今天学了什么？
今天正式踏出了项目开发的第一步，核心学习了三件事：
1. **Python虚拟环境 (venv)**：如何给项目创建一个独立的“小隔间”，避免不同项目之间的库冲突。
2. **环境变量 (.env)**：如何把密码（API Key）藏在代码之外。
3. **HTTP请求与AI API**：如何用Python代码像浏览器一样向互联网上的AI大模型“发消息”，并收到回复。（这是最新的收获）

### ② 它是什么？（我的理解）
- **虚拟环境**：就像厨房里专门用来切菜的砧板，跟洗碗池（系统全局环境）分开。我在砧板上剁蒜（装`requests`库），不会弄脏整个厨房。
- **API调用**：就像我打电话给智谱AI公司的客服。我拨号（`requests.post`），说“请说一句关于AI安全的话”，客服（大模型）就在电话那头回答我。`API Key` 就是我拨打客服热线时需要的“会员密码”。
- **.env文件**：就像我的私人密码本。我把它放在电脑桌面上，但绝不把它夹在交给评委的“实验报告”（GitHub仓库）里。

### ③ 我今天具体做了什么？（操作清单）
- [x] 安装了Python 3.11，并勾选了“Add to PATH”。
- [x] 在VS Code中创建了项目文件夹 `ai-security-hub`。
- [x] 在终端中执行 `python -m venv venv` 创建了虚拟环境，并成功激活（看到 `(venv)` 出现）。
- [x] 安装了 `requests` 和 `python-dotenv` 两个库。
- [x] 编写了 `day1_hello_ai.py` 文件。（根据ds进行复制但看懂了主体代码）
- [x] 创建了 `.env` 文件，写入了从智谱AI官网复制的API Key。
- [x] 在终端运行 `python day1_hello_ai.py`，成功看到了AI回复的关于AI安全漏洞的解释。

### ④ 我踩了哪些坑？怎么爬出来的？
- **坑1：不知道该在哪里创建文件。**
  - *现象*：对着VS Code界面发懵，找不到新建文件的按钮，差点在Windows资源管理器里创建txt改后缀。区分了文件包folder和文件file的不同。
  - *解决*：学会了使用 `Ctrl + N` 新建文件，`Ctrl + S` 保存并命名（一定要加 `.py` 后缀）。
  
- **坑2：运行代码后终端一片空白，没有任何输出。**
  - *现象*：明明按了回车，但光标直接跳到下一行，没有任何打印文字。
  - *解决*：检查后发现是代码里的 `try...except...` 缩进没有对齐，导致 `print` 语句根本没有被执行。把代码重新排版（对齐）后就正常了。

- **坑3（未发生，但特别注意）：差点把 `.env` 传上网。**
  - *警戒*：记住了绝对不能把存有真实密码的 `.env` 文件上传到GitHub。我创建了 `.gitignore` 文件把它屏蔽了。

### 💡 今日核心感悟
以前觉得“调用大模型”是特别高大上的黑科技，今天亲手写代码跑通后才发现，本质上就是**发起一个网络请求、接收一段JSON数据**。我离AI开发的大门又近了一步。

### 🚀 明日作战计划
Day 2 我要去NVD（美国国家漏洞数据库）抓取真实的AI安全漏洞编号，开始搭建“情报监测模块”。
